# Template environment variables Desa Kuniran.
# File ini AMAN untuk di-commit (tidak berisi secret asli).
# Salin ke .env lokal untuk isi nilai sebenarnya (.env di-gitignore, tidak pernah dikirim ke repo).
